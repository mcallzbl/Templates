project_name:
  type: str
  help: Project name
project_slug:
  type: str
  default: "{{ project_name|lower|replace(' ', '-') }}"
package_name:
  type: str
  default: "{{ project_slug|replace('-', '_') }}"
author:
  type: str
  default: Your Name
